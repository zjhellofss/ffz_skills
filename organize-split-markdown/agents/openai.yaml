interface:
  display_name: "整理并切分 Markdown"
  short_description: "分阶段规范 Markdown 格式并按语义切分文档"
  default_prompt: "Use $organize-split-markdown to standardize this Markdown document and split it into semantically complete files."
